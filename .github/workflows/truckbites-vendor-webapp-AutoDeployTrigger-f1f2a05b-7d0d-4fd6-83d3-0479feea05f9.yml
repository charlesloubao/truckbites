name: Trigger auto deployment for truckbites-vendor-webapp

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/truckbites-vendor-webapp-AutoDeployTrigger-f1f2a05b-7d0d-4fd6-83d3-0479feea05f9.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.TRUCKBITESVENDORWEBAPP_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.TRUCKBITESVENDORWEBAPP_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.TRUCKBITESVENDORWEBAPP_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}Eleven95.TruckBites.Vendor.WebApp/Eleven95.TruckBites.Vendor.WebApp
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: eleven95truckbites.azurecr.io
          registryUsername: ${{ secrets.TRUCKBITESVENDORWEBAPP_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.TRUCKBITESVENDORWEBAPP_REGISTRY_PASSWORD }}
          containerAppName: truckbites-vendor-webapp
          resourceGroup: TruckBites
          imageToBuild: eleven95truckbites.azurecr.io/truckbites-vendor-webapp:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


